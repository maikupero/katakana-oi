version: '3.3'

services:
  db:
    container_name: katakanaDB
    tty: true
    image: mariadb
    env_file:
      - .env
    environment:
      TZ: US/Eastern
    ports:
      - '${MYSQL_PORT}:${MYSQL_PORT}'
    # expose:
    #   # Opens port on the container
    #   - '${MYSQL_PORT}'



